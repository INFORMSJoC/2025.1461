# Generated results

`test_primalFR` writes its timestamped MATLAB result files (`.mat`) and
matching diary logs (`.txt`) to this directory.

These files are generated experiment outputs and are not required to run the
code.
